[build]
buildCommand = "npm install && npm run build"

[deploy]
startCommand = "node packages/mcp/dist/http.js"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
